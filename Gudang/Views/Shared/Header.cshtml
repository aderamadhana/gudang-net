@{
    ViewData["ApplicationTitle"] = "APLIKASI GUDANG";
}
<nav class="topbar py-2">
    <div class="container-fluid px-3">
        <div class="d-flex align-items-center justify-content-between">
            <div class="d-flex align-items-center gap-3">
                <button class="btn btn-outline-light d-lg-none" data-bs-toggle="offcanvas" data-bs-target="#offcanvasSidebar" aria-label="Toggle menu">
                    <i class="bi bi-list"></i>
                </button>
                <span class="fw-bold">@ViewData["ApplicationTitle"]</span>
            </div>
            <!-- Profil dropdown -->
            <div class="dropdown">
                <button class="btn d-flex align-items-center gap-2" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="https://i.pravatar.cc/40?img=3" class="rounded-circle border" width="40" height="40" alt="avatar">
                    <span class="d-none d-sm-inline fw-medium text-white">ADE RAMADHANA PRATAMA</span>
                    <i class="bi bi-caret-down-fill small text-white"></i>
                </button>
                <ul class="dropdown-menu dropdown-menu-end shadow">
                    <li class="px-3 py-2">
                        <div class="fw-semibold">ADE RAMADHANA PRATAMA</div>
                        <div class="text-secondary small">Pegawai</div>
                    </li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i>Profil</a></li>
                    <li>
                        <form action="/logout" method="post" class="px-2 my-1">
                            <button type="submit" class="btn btn-dark w-100">
                                <i class="bi bi-box-arrow-right me-2"></i>Logout
                            </button>
                        </form>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</nav>